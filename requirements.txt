# Common
numpy>=1.21.0,<2.0.0
pandas>=1.5.0
python-dotenv>=1.0.0
tqdm>=4.0.0

# Aspect extraction (inference)
torch>=1.10.0
transformers>=4.30.0
kiwipiepy>=0.15.0
dask>=2023.0.0

# Aspect normalization & type determination (OpenAI)
openai>=2.0.0

# Sentiment analysis (GRU)
tensorflow==2.15.0
python-mecab-ko>=1.3.7

# Aspect contribution (Random Forest + SHAP)
scikit-learn>=1.0.0
shap>=0.42.0
